# 2026-03-26

- 视图信息页 `/clients/:clientKey/views` 新增 `Tree / 2D / 3D` 三模式渲染切换，默认展示 2D。
- 2D/3D 渲染直接基于 `/v2/ui-tree/snapshot` 的 `roots` 节点树扁平化结果，不新增网关字段依赖。
- 页面新增节点统计信息（`nodes`、`depth`），用于快速感知快照规模。
- 修复 `src/styles.css` 末尾多余花括号，并补充视图模式与画布样式。
- UI 测试 `tests/ui/ViewInfoPage.test.tsx` 已覆盖模式切换（2D -> 3D -> Tree）。
- 验证通过：`npm test -- tests/ui/ViewInfoPage.test.tsx`、`npm run build`。
- 修复 H5 前端 `Failed to fetch`：开发环境新增 Vite 代理 `/__neptune_gateway -> http://127.0.0.1:18765`，并在 `fetchJson` 中自动将本地网关请求改写为同源代理地址，规避浏览器 CORS 拦截。
- 网络异常错误文案升级为可读提示：明确“网关不可达或跨域限制”。
- 验证通过：`npm test -- tests/viewService.test.ts tests/ui/ViewInfoPage.test.tsx`、`npm run build`。
- 视图快照 schema/types 升级支持 V2 字段：`frame/style/text/visible`。
- 2D/3D 渲染改为优先使用 `frame` 进行绝对定位和尺寸还原，并叠加 `style/text` 的基础视觉映射；缺失 `frame` 时回退旧树深布局。
- 验证通过：`npm test -- tests/viewService.test.ts tests/ui/ViewInfoPage.test.tsx`、`npm run build`。
